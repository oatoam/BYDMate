<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_container"
    tools:context=".MainActivity">

    <!-- 主界面区域 -->
    <com.toddmo.bydmate.client.widgets.ApplicationPIPView
        android:id="@+id/primary_region"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/divider_vertical"
        app:layout_constraintBottom_toTopOf="@id/floating_window_placeholder"
        app:pipInstanceId="main_pip" />

    <!-- 副界面区域 -->
    <FrameLayout
        android:id="@+id/secondary_region"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toEndOf="@id/divider_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/floating_window_placeholder">

        <!-- 信息栏 -->
        <com.toddmo.bydmate.client.widgets.InfoBar
            android:id="@+id/info_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top" />

        <!-- 副ApplicationPIPView -->
        <com.toddmo.bydmate.client.widgets.ApplicationPIPView
            android:id="@+id/secondary_pip_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="?attr/actionBarSize"
            app:pipInstanceId="secondary_pip" />

    </FrameLayout>

    <!-- 垂直调节杆 -->
    <View
        android:id="@+id/divider_vertical"
        android:layout_width="20dp"
        android:layout_height="0dp"
        android:background="@android:color/white"
        app:layout_constraintStart_toEndOf="@id/primary_region"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/floating_window_placeholder" />

    <!-- 水平调节杆 -->
    <View
        android:id="@+id/divider_horizontal"
        android:layout_width="0dp"
        android:layout_height="20dp"
        android:background="@android:color/white"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:visibility="gone" />

    <!-- 悬浮窗占位符 -->
    <View
        android:id="@+id/floating_window_placeholder"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:visibility="invisible" />

</androidx.constraintlayout.widget.ConstraintLayout>